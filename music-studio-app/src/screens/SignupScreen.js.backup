import React, { useState, useEffect, useRef } from 'react';
import {
    View,
    Text,
    StyleSheet,
    Animated,
    TouchableWithoutFeedback,
    Dimensions,
    Platform,
    Alert
} from 'react-native';
import { LinearGradient } from 'expo-linear-gradient';
import { useAuth } from '../contexts/AuthContext';
import EnhancedAnimatedInput from '../components/EnhancedAnimatedInput';
import AnimatedMusicButton from '../components/AnimatedMusicButton';
import FloatingNotes from '../components/FloatingNotes';
import UISounds from '../utils/UISounds';

const { width, height } = Dimensions.get('window');

export default function SignupScreen({ navigation }) {
    const [email, setEmail] = useState('');
    const [password, setPassword] = useState('');
    const [confirmPassword, setConfirmPassword] = useState('');
    const [loading, setLoading] = useState(false);
    const [errorMsg, setErrorMsg] = useState('');
    const { signUp } = useAuth();

    // Animation values
    const fadeAnim = useRef(new Animated.Value(0)).current;
    const titleAnim = useRef(new Animated.Value(-50)).current;
    const logoAnim = useRef(new Animated.Value(0)).current;
    const formAnim = useRef(new Animated.Value(50)).current;
    const pulseAnim = useRef(new Animated.Value(1)).current;
    const logoRotate = useRef(new Animated.Value(0)).current;

    // Ripple effect state
    const [ripples, setRipples] = useState([]);

    useEffect(() => {
        UISounds.init();

        Animated.parallel([
            Animated.timing(fadeAnim, {
                toValue: 1,
                duration: 800,
                useNativeDriver: true,
            }),
            Animated.spring(titleAnim, {
                toValue: 0,
                tension: 20,
                friction: 7,
                useNativeDriver: true,
            }),
            Animated.spring(formAnim, {
                toValue: 0,
                tension: 20,
                friction: 7,
                delay: 300,
                useNativeDriver: true,
            }),
            Animated.spring(logoAnim, {
                toValue: 1,
                tension: 20,
                friction: 7,
                delay: 150,
                useNativeDriver: true,
            }),
        ]).start();

        // Logo pulse animation
        Animated.loop(
            Animated.sequence([
                Animated.timing(pulseAnim, {
                    toValue: 1.15,
                    duration: 1500,
                    useNativeDriver: true,
                }),
                Animated.timing(pulseAnim, {
                    toValue: 1,
                    duration: 1500,
                    useNativeDriver: true,
                }),
            ])
        ).start();

        // Logo rotation
        Animated.loop(
            Animated.timing(logoRotate, {
                toValue: 1,
                duration: 8000,
                useNativeDriver: true,
            })
        ).start();
    }, []);

    const handleSignup = async () => {
        if (password !== confirmPassword) {
            setErrorMsg('Passwords do not match');
            UISounds.playError();
            return;
        }

        setLoading(true);
        setErrorMsg('');
        try {
            const { error } = await signUp(email, password);
            if (error) {
                setErrorMsg(error.message);
                UISounds.playError();
            } else {
                UISounds.playSuccess();
                if (Platform.OS === 'web') {
                    window.alert('Success! Check your email for the confirmation link!');
                } else {
                    Alert.alert('Success', 'Check your email for the confirmation link!');
                }
                navigation.navigate('Login');
            }
        } catch (err) {
            setErrorMsg(err.message);
            UISounds.playError();
        } finally {
            setLoading(false);
        }
    };

    const handleScreenTap = (event) => {
        const sounds = ['playBell', 'playWood', 'playMetal', 'playString', 'playWind'];
        const randomSound = sounds[Math.floor(Math.random() * sounds.length)];
        const randomFreq = 400 + Math.random() * 600;
        UISounds[randomSound](randomFreq, 100);

        if (event.nativeEvent) {
            const { locationX, locationY } = event.nativeEvent;
            const newRipple = {
                id: Date.now(),
                x: locationX,
                y: locationY,
                anim: new Animated.Value(0),
            };

            setRipples(prev => [...prev, newRipple]);

            Animated.timing(newRipple.anim, {
                toValue: 1,
                duration: 1000,
                useNativeDriver: true,
            }).start(() => {
                setRipples(prev => prev.filter(r => r.id !== newRipple.id));
            });
        }
    };

    const logoSpin = logoRotate.interpolate({
        inputRange: [0, 1],
        outputRange: ['0deg', '360deg'],
    });

    return (
        <TouchableWithoutFeedback onPress={handleScreenTap}>
            <View style={styles.container}>
                <LinearGradient
                    colors={['#0a0000', '#1a0a0a', '#2d0a0a', '#0a0000']}
                    style={styles.gradient}
                    start={{ x: 0, y: 0 }}
                    end={{ x: 1, y: 1 }}
                >
                    <FloatingNotes />

                    {ripples.map(ripple => (
                        <Animated.View
                            key={ripple.id}
                            style={[
                                styles.ripple,
                                {
                                    left: ripple.x - 50,
                                    top: ripple.y - 50,
                                    opacity: ripple.anim.interpolate({
                                        inputRange: [0, 1],
                                        outputRange: [0.6, 0],
                                    }),
                                    transform: [{
                                        scale: ripple.anim.interpolate({
                                            inputRange: [0, 1],
                                            outputRange: [0, 3],
                                        }),
                                    }],
                                },
                            ]}
                        />
                    ))}

                    <Animated.View
                        style={[
                            styles.content,
                            { opacity: fadeAnim }
                        ]}
                    >
                        {/* Extraordinary UJ Logo - Ugant & Jessica */}
                        <Animated.View
                            style={[
                                styles.logoContainer,
                                {
                                    transform: [
                                        { scale: Animated.multiply(logoAnim, pulseAnim) },
                                    ],
                                },
                            ]}
                        >
                            {/* Outer glow ring */}
                            <Animated.View
                                style={[
                                    styles.outerRing,
                                    {
                                        transform: [{ rotate: logoSpin }],
                                        opacity: pulseAnim.interpolate({
                                            inputRange: [1, 1.15],
                                            outputRange: [0.3, 0.6],
                                        }),
                                    }
                                ]}
                            />

                            {/* Middle ring - heartbeat effect */}
                            <Animated.View
                                style={[
                                    styles.middleRing,
                                    {
                                        transform: [
                                            { rotate: logoSpin },
                                            { scale: pulseAnim }
                                        ],
                                    }
                                ]}
                            />

                            {/* Main logo circle with gradient */}
                            <LinearGradient
                                colors={['#FF4500', '#FF6347', '#DC143C', '#8B0000']}
                                style={styles.logoCircle}
                                start={{ x: 0, y: 0 }}
                                end={{ x: 1, y: 1 }}
                            >
                                {/* Inner black accent */}
                                <View style={styles.innerBlackRing} />

                                {/* UJ Text with 3D effect */}
                                <View style={styles.ujContainer}>
                                    <Text style={styles.logoTextShadow}>UJ</Text>
                                    <Text style={styles.logoText}>UJ</Text>
                                </View>

                                {/* Heartbeat line */}
                                <Animated.View
                                    style={[
                                        styles.heartbeatLine,
                                        {
                                            opacity: pulseAnim.interpolate({
                                                inputRange: [1, 1.15],
                                                outputRange: [0.5, 1],
                                            }),
                                        }
                                    ]}
                                >
                                    <View style={styles.heartbeatPath} />
                                </Animated.View>

                                {/* Floating hearts */}
                                <Animated.Text
                                    style={[
                                        styles.floatingHeart,
                                        styles.heart1,
                                        {
                                            transform: [{
                                                translateY: pulseAnim.interpolate({
                                                    inputRange: [1, 1.15],
                                                    outputRange: [0, -5],
                                                }),
                                            }],
                                        }
                                    ]}
                                >
                                    ❤️
                                </Animated.Text>
                                <Animated.Text
                                    style={[
                                        styles.floatingHeart,
                                        styles.heart2,
                                        {
                                            transform: [{
                                                translateY: pulseAnim.interpolate({
                                                    inputRange: [1, 1.15],
                                                    outputRange: [0, 5],
                                                }),
                                            }],
                                        }
                                    ]}
                            <Animated.View
                                    style={[
                                        styles.particle,
                                        styles.particle1,
                                        {
                                            opacity: pulseAnim.interpolate({
                                                inputRange: [1, 1.15],
                                                outputRange: [0.3, 0.8],
                                            }),
                                        }
                                    ]}
                                />
                                <Animated.View
                                    style={[
                                        styles.particle,
                                        styles.particle2,
                                        {
                                            opacity: pulseAnim.interpolate({
                                                inputRange: [1, 1.15],
                                                outputRange: [0.4, 0.9],
                                            }),
                                        }
                                    ]}
                                />
                                <Animated.View
                                    style={[
                                        styles.particle,
                                        styles.particle3,
                                        {
                                            opacity: pulseAnim.interpolate({
                                                inputRange: [1, 1.15],
                                                outputRange: [0.2, 0.7],
                                            }),
                                        }
                                    ]}
                                />
                        </Animated.View>

                        {/* Love dedication text */}
                        <Animated.View
                            style={{
                                transform: [{ translateY: titleAnim }],
                            }}
                        >
                            <Text style={styles.title}>Ugant & Jessica</Text>
                            <Text style={styles.subtitle}>❤️ Where Two Hearts Create One Melody ❤️</Text>
                            <Text style={styles.dedication}>Forever Yours</Text>
                        </Animated.View>

                        {/* Form */}
                        <Animated.View
                            style={[
                                styles.formContainer,
                                {
                                    transform: [{ translateY: formAnim }],
                                },
                            ]}
                        >
                            <EnhancedAnimatedInput
                                label="Email"
                                value={email}
                                onChangeText={setEmail}
                                keyboardType="email-address"
                                autoCapitalize="none"
                                useEnhancedSounds={true}
                            />

                            <EnhancedAnimatedInput
                                label="Password"
                                value={password}
                                onChangeText={setPassword}
                                secureTextEntry
                                autoCapitalize="none"
                                useEnhancedSounds={true}
                            />

                            <EnhancedAnimatedInput
                                label="Confirm Password"
                                value={confirmPassword}
                                onChangeText={setConfirmPassword}
                                secureTextEntry
                                autoCapitalize="none"
                                useEnhancedSounds={true}
                            />

                            {errorMsg ? (
                                <Animated.View style={styles.errorContainer}>
                                    <Text style={styles.errorText}>⚠️ {errorMsg}</Text>
                                </Animated.View>
                            ) : null}

                            <AnimatedMusicButton
                                title="Create Account"
                                onPress={handleSignup}
                                disabled={loading}
                                loading={loading}
                            />

                            <TouchableWithoutFeedback
                                onPress={() => {
                                    UISounds.playTap();
                                    if (navigation && navigation.navigate) {
                                        navigation.navigate('Login');
                                    }
                                }}
                            >
                                <View style={styles.loginContainer}>
                                    <Text style={styles.loginText}>
                                        Already have an account?{' '}
                                        <Text style={styles.loginLink}>Login</Text>
                                    </Text>
                                </View>
                            </TouchableWithoutFeedback>
                        </Animated.View>

                        {/* Sound Wave */}
                        <View style={styles.soundWaveContainer}>
                            <Text style={styles.soundWaveText}>∿∿∿ Tap to play musical sounds ∿∿∿</Text>
                        </View>
                    </Animated.View>
                </LinearGradient>
            </View>
        </TouchableWithoutFeedback>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    gradient: {
        flex: 1,
    },
    content: {
        flex: 1,
        justifyContent: 'center',
        paddingHorizontal: 40,
        maxWidth: 450,
        alignSelf: 'center',
        width: '100%',
    },
    logoContainer: {
        alignSelf: 'center',
        marginBottom: 25,
        position: 'relative',
    },
    outerRing: {
        position: 'absolute',
        width: 140,
        height: 140,
        borderRadius: 70,
        borderWidth: 3,
        borderColor: '#FF4500',
        top: -20,
        left: -20,
        shadowColor: '#FF4500',
        shadowOffset: { width: 0, height: 0 },
        shadowOpacity: 0.8,
        shadowRadius: 25,
    },
    middleRing: {
        position: 'absolute',
        width: 120,
        height: 120,
        borderRadius: 60,
        borderWidth: 2,
        borderColor: '#DC143C',
        top: -10,
        left: -10,
        shadowColor: '#DC143C',
        shadowOffset: { width: 0, height: 0 },
        shadowOpacity: 0.6,
        shadowRadius: 20,
    },
    logoCircle: {
        width: 100,
        height: 100,
        borderRadius: 50,
        justifyContent: 'center',
        alignItems: 'center',
        borderWidth: 4,
        borderColor: '#000',
        shadowColor: '#FF4500',
        shadowOffset: { width: 0, height: 0 },
        shadowOpacity: 1,
        shadowRadius: 30,
        elevation: 15,
        overflow: 'visible',
    },
    innerBlackRing: {
        position: 'absolute',
        width: 85,
        height: 85,
        borderRadius: 42.5,
        borderWidth: 2,
        borderColor: 'rgba(0, 0, 0, 0.5)',
    },
    ujContainer: {
        position: 'relative',
        zIndex: 2,
    },
    logoTextShadow: {
        position: 'absolute',
        fontSize: 42,
        fontWeight: '900',
        color: '#000',
        letterSpacing: 3,
        top: 2,
        left: 2,
        textShadowColor: 'rgba(0, 0, 0, 0.8)',
        textShadowOffset: { width: 3, height: 3 },
        textShadowRadius: 6,
    },
    logoText: {
        fontSize: 42,
        fontWeight: '900',
        color: '#fff',
        letterSpacing: 3,
        textShadowColor: '#FFD700',
        textShadowOffset: { width: 0, height: 0 },
        textShadowRadius: 10,
        zIndex: 3,
    },
    heartbeatLine: {
        position: 'absolute',
        bottom: 10,
        width: 60,
        height: 2,
        justifyContent: 'center',
        alignItems: 'center',
    },
    heartbeatPath: {
        width: '100%',
        height: 2,
        backgroundColor: '#FFD700',
        shadowColor: '#FFD700',
        shadowOffset: { width: 0, height: 0 },
        shadowOpacity: 1,
        shadowRadius: 5,
    },
    floatingHeart: {
        position: 'absolute',
        fontSize: 16,
    },
    heart1: {
        top: -25,
        right: -15,
    },
    heart2: {
        bottom: -25,
        left: -15,
    },
    orbitingNote: {
        position: 'absolute',
        fontSize: 18,
        color: '#FFD700',
        fontWeight: 'bold',
        textShadowColor: '#FFD700',
        textShadowOffset: { width: 0, height: 0 },
        textShadowRadius: 10,
    },
    particle: {
        position: 'absolute',
        width: 6,
        height: 6,
        borderRadius: 3,
        backgroundColor: '#FF4500',
        shadowColor: '#FF4500',
        shadowOffset: { width: 0, height: 0 },
        shadowOpacity: 1,
        shadowRadius: 8,
    },
    particle1: {
        top: -30,
        left: 30,
    },
    particle2: {
        top: 50,
        right: -20,
    },
    particle3: {
        bottom: 20,
        left: -15,
    },
    musicNoteContainer: {
        display: 'none',
    },
    musicNote: {
        display: 'none',
    },
    title: {
        fontSize: 28,
        fontWeight: 'bold',
        color: '#FF4500',
        textAlign: 'center',
        marginBottom: 5,
        textShadowColor: '#FF4500',
        textShadowOffset: { width: 0, height: 0 },
        textShadowRadius: 20,
        letterSpacing: 2,
    },
    subtitle: {
        fontSize: 13,
        color: '#FFD700',
        textAlign: 'center',
        marginBottom: 8,
        fontStyle: 'italic',
        fontWeight: '600',
    },
    dedication: {
        fontSize: 11,
        color: '#DC143C',
        textAlign: 'center',
        marginBottom: 25,
        fontStyle: 'italic',
        letterSpacing: 1,
        textShadowColor: '#DC143C',
        textShadowOffset: { width: 0, height: 0 },
        textShadowRadius: 8,
    },
    formContainer: {
        marginTop: 10,
    },
    errorContainer: {
        backgroundColor: 'rgba(255, 0, 0, 0.1)',
        borderRadius: 8,
        padding: 12,
        marginBottom: 15,
        borderWidth: 1,
        borderColor: 'rgba(255, 0, 0, 0.3)',
    },
    errorText: {
        color: '#ff6b6b',
        textAlign: 'center',
        fontSize: 14,
    },
    loginContainer: {
        marginTop: 25,
        alignItems: 'center',
    },
    loginText: {
        color: '#999',
        fontSize: 15,
    },
    loginLink: {
        color: '#FF6B9D',
        fontWeight: 'bold',
        textDecorationLine: 'underline',
    },
    soundWaveContainer: {
        position: 'absolute',
        bottom: 30,
        left: 0,
        right: 0,
        alignItems: 'center',
    },
    soundWaveText: {
        color: 'rgba(255, 107, 157, 0.5)',
        fontSize: 14,
        fontStyle: 'italic',
    },
    ripple: {
        position: 'absolute',
        width: 100,
        height: 100,
        borderRadius: 50,
        borderWidth: 2,
        borderColor: '#FF6B9D',
        pointerEvents: 'none',
    },
});
